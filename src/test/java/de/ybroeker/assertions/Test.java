package de.ybroeker.assertions;

import java.util.logging.Logger;

/**
 * @author ybroeker
 */
public class Test {
    private static final Logger LOG = Logger.getLogger(Test.class.getName());


/*    public void test() {
        Comparator<Integer> comparator = Integer::compare;

        assertThatComparedWith(comparator).$(0).isLowerThan(1);
        assertComparedWith(comparator).that(0).isLowerThan(1);


        assertThat(comparator).results(1).greaterThan(2);
        assertThat(comparator).implies(1).greaterThan(2);
        assertThat(comparator).calcs(1).greaterThan(2);


        assertThat(1).comparedWith(comparator).isGreaterThan(0);
        assertThat(1).comparingWith(comparator).isGreaterThan(0);
        assertThat(1).usingComparator(comparator).isLessThan(2);

    }
*/

}
